<template><div class="space-y-2"><div class="flex justify-between items-center"><div class="font-semibold">{{ title }} <span class="ml-2 text-xs text-slate-500">{{ tab }}</span> <span class="text-xs">| {{ rows.length }}</span></div><button class="text-xs">See All</button></div><div class="space-y-2"><DailyStatusRow v-for="r in rows" :key="`${type}-${r.reservation_id}`" :row="r" :type="type" @action="$emit('action',r)"/></div></div></template>
<script setup>import DailyStatusRow from './DailyStatusRow.vue'; defineProps({title:String,tab:String,rows:Array,type:String}); defineEmits(['action']);</script>
